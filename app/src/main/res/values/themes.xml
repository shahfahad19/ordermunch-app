<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Ordermunch" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/primary</item>
    </style>

    <style name="Theme.Ordermunch" parent="Base.Theme.Ordermunch" />

    <style name="RoundedDialogTheme" parent="Theme.Material3.Light.Dialog">
        <item name="android:background">@drawable/rounded_dialog_bg</item>
    </style>
</resources>